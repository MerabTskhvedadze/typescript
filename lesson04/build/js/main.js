"use strict";
//* Literal types
//note: only John(value) is valid
let myName;
let userName;
userName = 'Amy';
//*Functions
const sum = (a, b) => a + b;
const logMsg = (message) => console.log(message);
logMsg('hello');
logMsg(false);
logMsg(5);
